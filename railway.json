{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'no build needed - zero deps'"
  },
  "deploy": {
    "runtime": "V2",
    "startCommand": "node server.js"
  }
}
